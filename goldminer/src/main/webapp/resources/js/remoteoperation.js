/**
 * Remote key press function
 */

var goldMiner = {
	pressLeft : function() {
		if (EPG.isPlaying) {
		}
	},
	pressRight : function() {
		if (EPG.isPlaying) {
		}
	},
	pressUp : function() {
		if (EPG.isPlaying) {
		}
	},
	pressDown : function() {
		if (EPG.isPlaying) {
			EPG.catchItem();
		}
	}
}
